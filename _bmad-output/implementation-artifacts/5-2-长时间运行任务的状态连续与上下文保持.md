# Story 5.2: 长时间运行任务的状态连续与上下文保持

Status: ready-for-dev

## Story

作为用户，
我希望长时间运行的编码任务在数小时级别执行期间保持状态连续、监听连续和上下文连续，
以便我不因运行时间长而丢失可见性。

## Acceptance Criteria

1. **Given** 任务已运行超过 1 小时
   **When** 用户查看任务详情
   **Then** 状态、最近活动和心跳信息仍然持续更新（FR25, NFR18）
   **And** 不因运行时间长而降级为"状态未知"

2. **Given** 执行监督器与控制面之间出现短时网络波动
   **When** 连接恢复
   **Then** 状态同步恢复，波动期间的事件不丢失
   **And** 控制面展示恢复后的最新状态而非波动前的缓存状态

3. **Given** 任务运行数小时
   **When** 用户查看执行轨迹
   **Then** 所有阶段、活动和产物的时间线完整可见
   **And** 不因数据量增长而导致页面加载超时

## Tasks / Subtasks

> **建议实现顺序：** Task 1（心跳系统）→ Task 2（状态可信度）→ Task 3（上下文保持）→ Task 4（长轨迹优化）→ Task 5（UI 集成）→ Task 6（回归测试）

### Task 1: 建立心跳系统（Heartbeat）

- [ ] 1.1 在 `prisma/schema.prisma` 中添加 `Heartbeat` 模型：

  ```prisma
  model Heartbeat {
    id                String    @id @default(cuid())
    executionSessionId String
    taskId            String
    agentRunId        String
    timestamp         DateTime  @default(now())
    status            String    // 当前任务状态快照
    currentStage     String?
    currentActivity   String?
    lastOutputHash   String?   // 最近输出的哈希，用于检测输出是否更新
    pid               Int?
    metadata          Json?
    createdAt        DateTime  @default(now())

    executionSession  ExecutionSession @relation(fields: [executionSessionId], references: [id], onDelete: Cascade)
    task              Task             @relation(fields: [taskId], references: [id], onDelete: Cascade)

    @@index([executionSessionId, timestamp])
    @@index([taskId, timestamp])
    @@map("heartbeats")
  }
  ```

- [ ] 1.2 运行 `pnpm prisma migrate dev --name add_heartbeat_model`

- [ ] 1.3 在 `src/lib/execution/heartbeat/recorder.ts` 中实现心跳记录器：

  ```typescript
  import { prisma } from "@/lib/db/client";

  export interface RecordHeartbeatParams {
    executionSessionId: string;
    taskId: string;
    agentRunId: string;
    status: string;
    currentStage?: string;
    currentActivity?: string;
    lastOutputHash?: string;
    pid?: number;
    metadata?: Record<string, unknown>;
  }

  export async function recordHeartbeat(
    params: RecordHeartbeatParams
  ): Promise<{ id: string }> {
    const heartbeat = await prisma.heartbeat.create({
      data: {
        executionSessionId: params.executionSessionId,
        taskId: params.taskId,
        agentRunId: params.agentRunId,
        status: params.status,
        currentStage: params.currentStage ?? null,
        currentActivity: params.currentActivity ?? null,
        lastOutputHash: params.lastOutputHash ?? null,
        pid: params.pid ?? null,
        metadata: (params.metadata ?? {}) as object,
      },
    });
    return { id: heartbeat.id };
  }
  ```

- [ ] 1.4 在 `src/lib/execution/heartbeat/detector.ts` 中实现心跳超时检测器：

  ```typescript
  import { prisma } from "@/lib/db/client";

  export interface HeartbeatTimeoutConfig {
    /** 心跳超时阈值（毫秒），默认 60 秒 */
    timeoutMs?: number;
  }

  export interface HeartbeatStatus {
    taskId: string;
    lastHeartbeatAt: Date | null;
    lastStatus: string | null;
    lastStage: string | null;
    lastActivity: string | null;
    isStale: boolean;
    staleDurationMs: number | null;
    confidence: "trusted" | "stale" | "unknown";
  }

  /**
   * 获取任务的心跳可信度状态。
   *
   * 规则（NFR3, NFR12, NFR39）：
   * - 最近 60 秒有心跳 → confidence = "trusted"
   * - 60 秒至 120 秒有心跳 → confidence = "stale"（可感知滞后）
   * - 超过 120 秒无心跳 → confidence = "unknown"（状态不可信）
   * - 任务未在运行 → confidence = "unknown"
   */
  export async function getHeartbeatStatus(
    taskId: string,
    config: HeartbeatTimeoutConfig = {}
  ): Promise<HeartbeatStatus> {
    const { timeoutMs = 60_000 } = config;
    const staleThresholdMs = timeoutMs * 2; // 120 秒后标记为 unknown

    const latestHeartbeat = await prisma.heartbeat.findFirst({
      where: { taskId },
      orderBy: { timestamp: "desc" },
      select: {
        timestamp: true,
        status: true,
        currentStage: true,
        currentActivity: true,
      },
    });

    if (!latestHeartbeat) {
      return {
        taskId,
        lastHeartbeatAt: null,
        lastStatus: null,
        lastStage: null,
        lastActivity: null,
        isStale: true,
        staleDurationMs: null,
        confidence: "unknown",
      };
    }

    const now = new Date();
    const ageMs = now.getTime() - latestHeartbeat.timestamp.getTime();

    return {
      taskId,
      lastHeartbeatAt: latestHeartbeat.timestamp,
      lastStatus: latestHeartbeat.status,
      lastStage: latestHeartbeat.currentStage ?? null,
      lastActivity: latestHeartbeat.currentActivity ?? null,
      isStale: ageMs > timeoutMs,
      staleDurationMs: ageMs,
      confidence: ageMs <= timeoutMs
        ? "trusted"
        : ageMs <= staleThresholdMs
        ? "stale"
        : "unknown",
    };
  }

  /**
   * 批量获取多个任务的心跳状态（用于列表页）。
   */
  export async function batchGetHeartbeatStatus(
    taskIds: string[],
    config: HeartbeatTimeoutConfig = {}
  ): Promise<Map<string, HeartbeatStatus>> {
    const results = await Promise.all(
      taskIds.map((id) => getHeartbeatStatus(id, config))
    );
    return new Map(results.map((r) => [r.taskId, r]));
  }
  ```

- [ ] 1.5 在 `src/lib/execution/heartbeat/scheduler.ts` 中实现心跳调度器：

  ```typescript
  /**
   * 心跳调度器 — 在执行监督器进程内定期记录心跳。
   *
   * 职责（FR35, NFR3, NFR12, NFR18）：
   * - 定期向 Heartbeat 表写入心跳记录（默认间隔 30 秒）
   * - 捕获当前任务状态快照（状态、阶段、最近活动）
   * - 记录 agent 进程的 PID（用于进程存活检测）
   * - 在 session 结束时自动停止
   */

  import { recordHeartbeat, type RecordHeartbeatParams } from "./recorder";
  import { getProcessInfo } from "./process-info";

  export class HeartbeatScheduler {
    private intervalId: ReturnType<typeof setInterval> | null = null;
    private sessionId: string;
    private taskId: string;
    private agentRunId: string;
    private initialStatus: string;
    private intervalMs: number;

    constructor(
      sessionId: string,
      taskId: string,
      agentRunId: string,
      initialStatus: string,
      intervalMs = 30_000 // 默认 30 秒
    ) {
      this.sessionId = sessionId;
      this.taskId = taskId;
      this.agentRunId = agentRunId;
      this.initialStatus = initialStatus;
      this.intervalMs = intervalMs;
    }

    /** 启动心跳调度 */
    start(initialSnapshot: RecordHeartbeatParams): void {
      if (this.intervalId !== null) return;

      // 立即记录一次心跳
      void this.record(initialSnapshot);

      this.intervalId = setInterval(() => {
        void this.record(initialSnapshot);
      }, this.intervalMs);
    }

    /** 停止心跳调度 */
    stop(): void {
      if (this.intervalId !== null) {
        clearInterval(this.intervalId);
        this.intervalId = null;
      }
    }

    /** 记录一次心跳 */
    private async record(snapshot: RecordHeartbeatParams): Promise<void> {
      try {
        const processInfo = await getProcessInfo();
        await recordHeartbeat({
          executionSessionId: this.sessionId,
          taskId: this.taskId,
          agentRunId: this.agentRunId,
          status: snapshot.status ?? this.initialStatus,
          currentStage: snapshot.currentStage,
          currentActivity: snapshot.currentActivity,
          lastOutputHash: snapshot.lastOutputHash,
          pid: processInfo?.pid,
          metadata: {
            hostname: processInfo?.hostname ?? process.env["HOSTNAME"] ?? "unknown",
          },
        });
      } catch (err) {
        // 心跳记录失败不应中断主流程
        console.error("[HeartbeatScheduler] Failed to record heartbeat:", err);
      }
    }

    /** 在主流程获取当前状态快照后调用，更新快照信息 */
    async recordWithSnapshot(snapshot: {
      status: string;
      currentStage?: string;
      currentActivity?: string;
      lastOutputHash?: string;
    }): Promise<void> {
      await this.record(snapshot);
    }
  }
  ```

- [ ] 1.6 在 `src/lib/execution/heartbeat/process-info.ts` 中实现进程信息获取：

  ```typescript
  /** 获取当前进程和父进程信息（用于心跳中的 PID 快照） */
  export async function getProcessInfo(): Promise<{
    pid: number;
    ppid: number;
    hostname: string;
  } | null> {
    try {
      const { pid, ppid } = process;
      const hostname = process.env["HOSTNAME"] ?? "unknown";
      return { pid, ppid, hostname };
    } catch {
      return null;
    }
  }
  ```

- [ ] 1.7 在 `src/lib/execution/heartbeat/index.ts` 导出所有公共 API：

  ```typescript
  export { recordHeartbeat, type RecordHeartbeatParams } from "./recorder";
  export {
    getHeartbeatStatus,
    batchGetHeartbeatStatus,
    type HeartbeatTimeoutConfig,
    type HeartbeatStatus,
  } from "./detector";
  export { HeartbeatScheduler } from "./scheduler";
  export { getProcessInfo } from "./process-info";
  ```

### Task 2: 实现状态可信度机制

- [ ] 2.1 在 `src/lib/execution/continuity/state-trust.ts` 中实现状态可信度模块：

  ```typescript
  /**
   * 状态可信度 — 长时间运行任务的核心信任机制。
   *
   * 规则（NFR3, NFR12, NFR36, NFR39）：
   * - 控制面展示的状态必须带有可信度标记
   * - "trusted"：最近有心跳，状态可信
   * - "stale"：心跳有延迟但未超时，用户可见但需提示
   * - "unknown"：心跳超时或任务不在运行，控制面必须显式暴露"状态不可信"
   * - 任何情况下不得在"unknown"状态下默认显示"成功"或"运行中"
   */

  import { getHeartbeatStatus, type HeartbeatStatus } from "@/lib/execution/heartbeat";

  export type StateConfidence = "trusted" | "stale" | "unknown";

  export interface StateTrustLevel {
    confidence: StateConfidence;
    heartbeatStatus: HeartbeatStatus;
    displayRecommendation: "show_normal" | "show_warning" | "show_stale" | "show_unknown";
    badgeText: { zh: string; en: string };
    badgeVariant: "default" | "secondary" | "destructive" | "outline";
  }

  /**
   * 根据任务 ID 和心跳状态计算状态可信度。
   */
  export async function computeStateTrust(
    taskId: string,
    currentStatus: string,
    config: { heartbeatTimeoutMs?: number } = {}
  ): Promise<StateTrustLevel> {
    const heartbeat = await getHeartbeatStatus(taskId, {
      timeoutMs: config.heartbeatTimeoutMs ?? 60_000,
    });

    const confidence = heartbeat.confidence;

    let displayRecommendation: StateTrustLevel["displayRecommendation"];
    let badgeText: StateTrustLevel["badgeText"];
    let badgeVariant: StateTrustLevel["badgeVariant"];

    switch (confidence) {
      case "trusted":
        displayRecommendation = "show_normal";
        badgeText = { zh: "可信", en: "Trusted" };
        badgeVariant = "default";
        break;

      case "stale":
        displayRecommendation = "show_stale";
        badgeText = { zh: "数据滞后", en: "Stale" };
        badgeVariant = "secondary";
        break;

      case "unknown":
      default:
        // 如果任务本就不是运行态（如已完成），"unknown" 心跳不代表问题
        const isRunningStatus = ["starting", "running", "waiting_for_input", "recovering"].includes(
          currentStatus
        );
        if (!isRunningStatus) {
          displayRecommendation = "show_normal";
          badgeText = { zh: "可信", en: "Trusted" };
          badgeVariant = "default";
        } else {
          displayRecommendation = "show_unknown";
          badgeText = { zh: "状态不可信", en: "Uncertain" };
          badgeVariant = "destructive";
        }
        break;
    }

    return { confidence, heartbeatStatus: heartbeat, displayRecommendation, badgeText, badgeVariant };
  }
  ```

- [ ] 2.2 在 `src/lib/execution/continuity/continuity-manager.ts` 中实现状态连续性管理器：

  ```typescript
  /**
   * 状态连续性管理器 — 负责在长时间运行任务中维护状态、上下文和监听三方面的连续性。
   *
   * 职责（FR25, NFR18, NFR33）：
   * - 状态连续：确保任务状态不会因时间长而丢失或降级
   * - 上下文保持：保留关键上下文快照，支持状态恢复
   * - 监听连续：确保日志和输出采集链路在长时间运行中不中断
   *
   * 上下文快照策略：
   * - 每 5 分钟记录一次完整的任务上下文快照到 Task.metadata
   * - 快照包含：currentStage、currentActivity、latestOutputOffset、产物列表
   * - 异常恢复时使用最近快照重建上下文
   */

  import { Prisma } from "@/lib/db/client";
  import { prisma } from "@/lib/db/client";

  const CONTEXT_SNAPSHOT_INTERVAL_MS = 5 * 60 * 1_000; // 5 分钟

  export interface ContinuitySnapshot {
    taskId: string;
    agentRunId: string;
    status: string;
    currentStage: string;
    currentActivity: string;
    nextStep: string;
    latestOutputOffset: number;
    artifactCount: number;
    lastHeartbeatAt: Date | null;
    snapshotAt: Date;
  }

  export interface ContinuityContext {
    latestSnapshot: ContinuitySnapshot | null;
    heartbeatConfidence: "trusted" | "stale" | "unknown";
    lastStateChangeAt: Date | null;
    continuousDurationMs: number | null;
  }

  /**
   * 获取任务的连续性上下文。
   */
  export async function getContinuityContext(
    taskId: string
  ): Promise<ContinuityContext> {
    const [task, latestHeartbeat] = await Promise.all([
      prisma.task.findUnique({
        where: { id: taskId },
        select: {
          status: true,
          currentStage: true,
          currentActivity: true,
          nextStep: true,
          updatedAt: true,
          metadata: true,
        },
      }),
      prisma.heartbeat.findFirst({
        where: { taskId },
        orderBy: { timestamp: "desc" },
        select: { timestamp: true },
      }),
    ]);

    if (!task) {
      return {
        latestSnapshot: null,
        heartbeatConfidence: "unknown",
        lastStateChangeAt: null,
        continuousDurationMs: null,
      };
    }

    const isRunning = ["starting", "running", "waiting_for_input", "recovering"].includes(
      task.status
    );

    let confidence: ContinuityContext["heartbeatConfidence"] = "unknown";
    if (latestHeartbeat) {
      const ageMs = Date.now() - latestHeartbeat.timestamp.getTime();
      confidence = ageMs <= 60_000 ? "trusted" : ageMs <= 120_000 ? "stale" : "unknown";
    }

    // 如果任务在运行且心跳未知超过阈值，连续运行时长不计算
    const continuousDurationMs =
      isRunning && latestHeartbeat
        ? Date.now() - latestHeartbeat.timestamp.getTime()
        : null;

    // 从 metadata 中提取最新快照
    const metadata = task.metadata as Record<string, unknown> | null;
    const snapshot = metadata?.["_lastContinuitySnapshot"] as {
      currentStage: string;
      currentActivity: string;
      nextStep: string;
      latestOutputOffset: number;
      artifactCount: number;
      snapshotAt: string;
    } | null;

    return {
      latestSnapshot: snapshot
        ? {
            taskId,
            agentRunId: "", // snapshot 不存储 agentRunId
            status: task.status,
            currentStage: task.currentStage,
            currentActivity: task.currentActivity,
            nextStep: task.nextStep,
            latestOutputOffset: snapshot.latestOutputOffset ?? 0,
            artifactCount: snapshot.artifactCount ?? 0,
            lastHeartbeatAt: latestHeartbeat?.timestamp ?? null,
            snapshotAt: new Date(snapshot.snapshotAt),
          }
        : null,
      heartbeatConfidence: confidence,
      lastStateChangeAt: task.updatedAt,
      continuousDurationMs,
    };
  }

  /**
   * 定期保存上下文快照（由 HeartbeatScheduler 在心跳时调用）。
   */
  export async function saveContinuitySnapshot(
    taskId: string,
    snapshot: {
      latestOutputOffset: number;
      artifactCount: number;
    }
  ): Promise<void> {
    const task = await prisma.task.findUnique({
      where: { id: taskId },
      select: {
        status: true,
        currentStage: true,
        currentActivity: true,
        nextStep: true,
        metadata: true,
      },
    });

    if (!task) return;

    // 只对运行中任务保存快照
    const isRunning = ["starting", "running", "waiting_for_input", "recovering"].includes(
      task.status
    );
    if (!isRunning) return;

    const now = new Date();
    const currentMetadata = (task.metadata ?? {}) as Record<string, unknown>;

    // 检查是否需要保存（每 5 分钟一次）
    const lastSnapshotAt = currentMetadata["_lastContinuitySnapshotAt"] as string | null;
    if (
      lastSnapshotAt &&
      now.getTime() - new Date(lastSnapshotAt).getTime() < CONTEXT_SNAPSHOT_INTERVAL_MS
    ) {
      return;
    }

    await prisma.task.update({
      where: { id: taskId },
      data: {
        metadata: {
          ...currentMetadata,
          _lastContinuitySnapshot: {
            currentStage: task.currentStage,
            currentActivity: task.currentActivity,
            nextStep: task.nextStep,
            latestOutputOffset: snapshot.latestOutputOffset,
            artifactCount: snapshot.artifactCount,
            snapshotAt: now.toISOString(),
          },
          _lastContinuitySnapshotAt: now.toISOString(),
        } as Prisma.InputJsonValue,
      },
    });
  }
  ```

- [ ] 2.3 在 `src/lib/execution/continuity/reconnection.ts` 中实现断线重连的状态恢复：

  ```typescript
  /**
   * 断线重连时的状态恢复。
   *
   * 场景（AC-2）：
   * - 执行监督器与控制面之间出现短时网络波动
   * - 连接恢复后，控制面需要获取最新的真实状态
   * - 不能展示波动前的缓存状态
   *
   * 策略：
   * - 强制从数据库重新拉取（bypass 缓存）
   * - 对比最近的心跳时间戳，确认数据新鲜度
   * - 必要时触发一次即时心跳写入
   */

  import { prisma } from "@/lib/db/client";
  import { getHeartbeatStatus } from "@/lib/execution/heartbeat";

  export interface ReconnectionState {
    taskId: string;
    status: string;
    currentStage: string;
    currentActivity: string;
    nextStep: string;
    updatedAt: Date;
    heartbeatFresh: boolean;
    confidence: "trusted" | "stale" | "unknown";
  }

  /**
   * 获取重连后的最新任务状态（绕过缓存，直接查 DB）。
   */
  export async function fetchReconnectionState(
    taskId: string
  ): Promise<ReconnectionState | null> {
    const [task, heartbeatStatus] = await Promise.all([
      // 直接查数据库（每次请求都是实时数据，无应用层缓存）
      prisma.task.findUnique({
        where: { id: taskId },
        select: {
          status: true,
          currentStage: true,
          currentActivity: true,
          nextStep: true,
          updatedAt: true,
        },
      }),
      getHeartbeatStatus(taskId),
    ]);

    if (!task) return null;

    return {
      taskId,
      status: task.status,
      currentStage: task.currentStage,
      currentActivity: task.currentActivity,
      nextStep: task.nextStep,
      updatedAt: task.updatedAt,
      heartbeatFresh: heartbeatStatus.confidence === "trusted",
      confidence: heartbeatStatus.confidence,
    };
  }
  ```

### Task 3: 打通心跳系统与执行监督器

- [ ] 3.1 更新 `src/lib/execution/supervisor/launch.ts` 中的 `launchTask()`：

  - 3.1.1 在 tmux session 创建成功并进入 running 状态后，启动 `HeartbeatScheduler`：

    ```typescript
    // 启动心跳调度器（在 session 成功创建后）
    const heartbeatScheduler = new HeartbeatScheduler(
      executionSession.id,
      input.taskId,
      input.agentRunId,
      "running",
      30_000 // 每 30 秒心跳一次
    );
    heartbeatScheduler.start({
      status: "running",
      currentStage: "运行中",
      currentActivity: "Agent 正在执行任务",
    });

    // 将 scheduler 绑定到 session，以便 lifecycle.ts 在结束时停止
    // 通过在 executionSession.metadata 中存储心跳相关元数据
    ```

  - 3.1.2 在 `ExecutionSession.metadata` 中记录心跳间隔：

    ```typescript
    metadata: {
      ...currentMetadata,
      heartbeatScheduler: {
        intervalMs: 30_000,
        startedAt: now.toISOString(),
        pid: tmuxResult.panePid,
      },
      activeExecutionSession: {
        id: executionSession.id,
        sessionName: sessionName,
        startedAt: now.toISOString(),
      },
    } as Prisma.InputJsonValue,
    ```

- [ ] 3.2 更新 `src/lib/execution/supervisor/lifecycle.ts` 中的 `endSession()`：

  - 3.2.1 在 session 结束时停止 `HeartbeatScheduler`：

    ```typescript
    // 从 session metadata 中获取并停止心跳调度器
    // 注意：HeartbeatScheduler 运行在监督器进程内存中，
    // 如果监督器进程重启，scheduler 会自动重新初始化
    // 关键是确保 session 状态被正确更新
    ```

  - 3.2.2 记录最后一次心跳（包含终止状态）：

    ```typescript
    // 在 endSession 中追加最后一次心跳
    await recordHeartbeat({
      executionSessionId: session.id,
      taskId: input.taskId,
      agentRunId: input.agentRunId,
      status: taskTargetStatus,
      currentStage: input.reason === "completed" ? "已完成" : "已终止",
      currentActivity: input.reason === "completed"
        ? "执行会话已正常结束。"
        : `执行会话已被终止：${input.reasonSummary ?? ""}`,
    });
    ```

- [ ] 3.3 在 `src/lib/execution/continuity/index.ts` 导出所有连续性相关 API：

  ```typescript
  export { computeStateTrust, type StateConfidence, type StateTrustLevel } from "./state-trust";
  export {
    getContinuityContext,
    saveContinuitySnapshot,
    type ContinuitySnapshot,
    type ContinuityContext,
  } from "./continuity-manager";
  export { fetchReconnectionState, type ReconnectionState } from "./reconnection";
  ```

### Task 4: 长轨迹优化（分页与虚拟滚动支持）

- [ ] 4.1 在 `src/lib/db/helpers.ts` 中添加长轨迹查询方法：

  ```typescript
  /**
   * 分页获取任务状态事件历史。
   * 用于长时间运行任务的状态流转时间线展示（AC-3）。
   *
   * 策略（架构要求 — 原始长日志不进入高频事务热表）：
   * - 使用游标分页（cursor-based），避免 OFFSET 性能退化
   * - 默认每页 50 条，支持自定义
   */
  export async function getTaskStateHistoryPaginated(
    taskId: string,
    options: {
      limit?: number;
      cursor?: string; // 上次查询的最后一条 event id
      direction?: "forward" | "backward";
      includeRejected?: boolean;
    } = {}
  ): Promise<{
    events: TaskStateEvent[];
    nextCursor: string | null;
    hasMore: boolean;
    total: number;
  }> {
    const { limit = 50, cursor, direction = "forward", includeRejected = false } = options;

    const where: Prisma.TaskStateEventWhereInput = { taskId };
    if (!includeRejected) where.rejected = false;

    const orderBy: Prisma.TaskStateEventOrderByWithRelationInput =
      direction === "forward" ? { createdAt: "asc" } : { createdAt: "desc" };

    const events = await prisma.taskStateEvent.findMany({
      where,
      orderBy,
      take: direction === "forward" ? limit + 1 : limit,
      ...(cursor
        ? {
            cursor: { id: cursor },
            skip: 1,
          }
        : {}),
    });

    const hasMore = events.length > limit;
    if (hasMore) events.pop();

    const total = await prisma.taskStateEvent.count({ where });

    return {
      events: direction === "forward" ? events : events.reverse(),
      nextCursor: hasMore && events.length > 0 ? events[events.length - 1].id : null,
      hasMore,
      total,
    };
  }

  /**
   * 获取任务执行轨迹摘要（用于长时间运行任务的状态流转展示）。
   */
  export async function getTaskExecutionTrail(
    taskId: string,
    options: { limit?: number } = {}
  ): Promise<{
    trail: Array<{
      id: string;
      type: "state_change" | "heartbeat" | "interaction" | "recovery";
      timestamp: Date;
      summary: string;
    }>;
    totalCount: number;
  }> {
    const { limit = 100 } = options;

    // 并行查询状态事件和心跳（心跳只取最新的 N 条作为采样）
    const [stateEvents, recentHeartbeats] = await Promise.all([
      prisma.taskStateEvent.findMany({
        where: { taskId, rejected: false },
        orderBy: { createdAt: "desc" },
        take: limit,
        select: {
          id: true,
          toStatus: true,
          trigger: true,
          reason: true,
          createdAt: true,
        },
      }),
      // 心跳只取每分钟一条采样（避免大量心跳记录导致查询爆炸）
      prisma.heartbeat.findMany({
        where: { taskId },
        orderBy: { timestamp: "desc" },
        // 取最近 1 小时或 limit 条，取较大值
        take: Math.max(limit, 60),
      }),
    ]);

    // 对心跳进行分钟级去重采样
    const heartbeatSamples: Array<{ id: string; timestamp: Date; summary: string }> = [];
    const seenMinutes = new Set<string>();
    for (const hb of recentHeartbeats) {
      const minuteKey = `${hb.timestamp.getFullYear()}-${String(hb.timestamp.getMonth()).padStart(2, '0')}-${String(hb.timestamp.getDate()).padStart(2, '0')}-${String(hb.timestamp.getHours()).padStart(2, '0')}-${String(hb.timestamp.getMinutes()).padStart(2, '0')}`;
      if (!seenMinutes.has(minuteKey)) {
        seenMinutes.add(minuteKey);
        heartbeatSamples.push({
          id: hb.id,
          timestamp: hb.timestamp,
          summary: hb.currentActivity ?? hb.status,
        });
      }
    }

    // 合并状态事件和心跳采样，按时间排序
    const stateTrail = stateEvents.map((e) => ({
      id: e.id,
      type: "state_change" as const,
      timestamp: e.createdAt,
      summary: `状态变更为 ${e.toStatus}${e.reason ? `（${e.reason}）` : ""}`,
    }));

    const heartbeatTrail = heartbeatSamples.map((hb) => ({
      id: hb.id,
      type: "heartbeat" as const,
      timestamp: hb.timestamp,
      summary: `心跳记录：${hb.summary}`,
    }));

    const merged = [...stateTrail, ...heartbeatTrail].sort(
      (a, b) => a.timestamp.getTime() - b.timestamp.getTime()
    );

    return {
      trail: merged.slice(-limit),
      totalCount: stateEvents.length + heartbeatSamples.length,
    };
  }
  ```

### Task 5: UI 集成 — 状态可信度与连续性展示

- [ ] 5.1 更新 `src/components/tasks/task-detail-view.tsx`：

  - 5.1.1 集成 `computeStateTrust()` 显示可信度标记：

    ```tsx
    import { computeStateTrust } from "@/lib/execution/continuity";
    import { StatusBadge } from "@/components/shared/status-badge";
    import { Clock, AlertTriangle } from "lucide-react";
    import { Badge } from "@/components/ui/badge";

    // 在任务状态区域添加可信度指示器
    const trustLevel = await computeStateTrust(task.id, task.status);

    return (
      <div className="flex items-center gap-2">
        {/* 状态标签 */}
        <StatusBadge status={task.status} />
        {/* 可信度指示器 */}
        {trustLevel.displayRecommendation === "show_stale" && (
          <Badge variant="secondary" className="text-xs">
            <Clock className="w-3 h-3 mr-1" />
            {trustLevel.badgeText.zh}
          </Badge>
        )}
        {trustLevel.displayRecommendation === "show_unknown" && (
          <Badge variant="destructive" className="text-xs">
            <AlertTriangle className="w-3 h-3 mr-1" />
            {trustLevel.badgeText.zh}
          </Badge>
        )}
      </div>
    );
    ```

  - 5.1.2 添加心跳时间显示（在状态区展示"最后心跳"）：

    ```tsx
    import { formatDistanceToNow } from "date-fns";
    import { zhCN } from "date-fns/locale";

    // 最后心跳时间（来自 trustLevel）
    {trustLevel.heartbeatStatus.lastHeartbeatAt && (
      <p className="text-xs text-muted-foreground">
        最后心跳：{formatDistanceToNow(trustLevel.heartbeatStatus.lastHeartbeatAt, {
          addSuffix: true,
          locale: zhCN,
        })}
      </p>
    )}
    ```

  - 5.1.3 当状态不可信时显示警告横幅：

    ```tsx
    import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
    import { AlertTriangle } from "lucide-react";

    {trustLevel.displayRecommendation === "show_unknown" && (
      <Alert variant="destructive">
        <AlertTriangle className="h-4 w-4" />
        <AlertTitle>状态不可信</AlertTitle>
        <AlertDescription>
          已超过 2 分钟未收到心跳，任务状态可能已过期。
          最后已知状态：{trustLevel.heartbeatStatus.lastStage ?? task.currentStage}
        </AlertDescription>
      </Alert>
    )}
    ```

- [ ] 5.2 更新 `src/components/tasks/state-timeline.tsx`：

  - 5.2.1 使用分页加载长轨迹（来自 `getTaskExecutionTrail`）：

    ```tsx
    import { formatDistanceToNow } from "date-fns";
    import { zhCN } from "date-fns/locale";
    import { Loader2 } from "lucide-react";

    interface TrailItem {
      id: string;
      type: "state_change" | "heartbeat" | "interaction" | "recovery";
      timestamp: Date;
      summary: string;
    }

    // 使用"加载更多"模式处理长轨迹
    const [trail, setTrail] = useState<TrailItem[]>([]);
    const [cursor, setCursor] = useState<string | null>(null);
    const [hasMore, setHasMore] = useState(true);
    const [isLoading, setIsLoading] = useState(false);

    async function loadMore() {
      if (isLoading || !hasMore) return;
      setIsLoading(true);
      const result = await getTaskExecutionTrail(taskId, {
        limit: 100,
      });
      setTrail(result.trail);
      setHasMore(result.totalCount > result.trail.length);
      setCursor(result.trail[result.trail.length - 1]?.id ?? null);
      setIsLoading(false);
    }
    ```

- [ ] 5.3 更新 `src/components/tasks/task-status-rail.tsx`（如果存在）或创建 `src/components/execution/status-rail.tsx`：

  - 5.3.1 展示阶段节点、当前高亮、最近活动时间、风险标记和下一步提示（UX-DR5）：

    ```tsx
    import { formatDistanceToNow } from "date-fns";
    import { zhCN } from "date-fns/locale";
    import { StatusBadge } from "@/components/shared/status-badge";

    type Confidence = "trusted" | "stale" | "unknown";

    interface StatusRailProps {
      status: string;
      currentStage: string;
      currentActivity: string;
      nextStep?: string | null;
      lastActivityAt?: Date | null;
      confidence: Confidence;
    }

    export function StatusRail({
      status,
      currentStage,
      currentActivity,
      nextStep,
      lastActivityAt,
      confidence,
    }: StatusRailProps) {
      return (
        <div className="flex flex-col gap-3">
          {/* 状态标签 */}
          <StatusBadge status={status} confidence={confidence} />

          {/* 当前阶段 */}
          <div>
            <span className="font-medium">{currentStage}</span>
            <p className="text-sm text-muted-foreground">{currentActivity}</p>
          </div>

          {/* 最近活动 */}
          {lastActivityAt && (
            <p className="text-xs text-muted-foreground">
              最近活动：{formatDistanceToNow(lastActivityAt, { addSuffix: true, locale: zhCN })}
            </p>
          )}

          {/* 下一步提示 */}
          {nextStep && (
            <div className="text-sm text-muted-foreground border-t pt-2">
              <span className="text-xs text-muted-foreground/60">下一步：</span>
              {nextStep}
            </div>
          )}
        </div>
      );
    }
    ```

### Task 6: 补充回归测试

- [ ] 6.1 在 `src/lib/execution/heartbeat/__tests__/recorder.test.ts` 中测试心跳记录器：

  ```typescript
  import { describe, it, expect, vi, beforeEach } from "vitest";
  import { recordHeartbeat } from "../recorder";

  describe("心跳记录器", () => {
    it("应创建心跳记录", async () => {
      // 测试 recordHeartbeat 正确写入数据库
    });

    it("应正确处理可选字段缺失", async () => {
      // 测试 pid、metadata 等可选字段为空时正确处理
    });
  });
  ```

- [ ] 6.2 在 `src/lib/execution/heartbeat/__tests__/detector.test.ts` 中测试心跳检测器：

  ```typescript
  describe("心跳可信度检测", () => {
    it("最近 60 秒内心跳应返回 trusted", async () => {
      // 创建一条新鲜心跳，验证置信度为 trusted
    });

    it("60-120 秒内心跳应返回 stale", async () => {
      // 创建一条超时心跳，验证置信度为 stale
    });

    it("超过 120 秒无心跳应返回 unknown", async () => {
      // 无心跳记录时，验证置信度为 unknown
    });

    it("非运行态任务不应显示 unknown", async () => {
      // completed/failed 等终态任务即使无心跳也应为 trusted
    });
  });
  ```

- [ ] 6.3 在 `src/lib/execution/__tests__/continuity.test.ts` 中测试连续性：

  ```typescript
  describe("状态连续性", () => {
    it("computeStateTrust 应返回正确的可信度", async () => {
      // 测试 state-trust 模块
    });

    it("getContinuityContext 应返回快照和心跳信息", async () => {
      // 测试连续性上下文
    });

    it("fetchReconnectionState 应绕过缓存返回最新状态", async () => {
      // 测试断线重连状态恢复
    });
  });
  ```

- [ ] 6.4 在 `src/lib/execution/__tests__/trail.test.ts` 中测试长轨迹查询：

  ```typescript
  describe("执行轨迹查询", () => {
    it("应正确分页状态事件", async () => {
      // 测试分页查询
    });

    it("心跳应进行分钟级去重采样", async () => {
      // 测试心跳采样逻辑
    });
  });
  ```

- [ ] 6.5 运行 `pnpm test` 确保所有测试通过

- [ ] 6.6 运行 `pnpm lint` 确保无 lint 错误

## Dev Notes

### 关键约束（来自 Checklist 自动分析）

> ⚠️ **这些约束必须严格遵守，违反将导致实现失败或破坏现有功能。**

1. **心跳调度器是监督器进程的内存组件**：当监督器进程重启时，HeartbeatScheduler 会重新初始化。状态真值永远来自数据库，不依赖进程内存。

2. **心跳不影响状态机**：心跳是状态机的补充观察机制，不是状态变更的触发条件。状态变更是唯一的真值来源。

3. **上下文快照不阻塞主流程**：`saveContinuitySnapshot` 每 5 分钟执行一次，且只在运行中任务上执行。任何失败都不应中断主执行链路。

4. **"状态不可信"必须显式暴露**：当 `confidence === "unknown"` 且任务在运行态时，UI 必须显示警告横幅（NFR39），不得默认展示"运行中"。

5. **长轨迹查询必须分页**：执行轨迹查询使用 cursor-based 分页，心跳采样在分钟级别去重，避免大量心跳记录导致性能退化。

6. **心跳写入失败不中断执行**：心跳记录失败只打印错误日志，不抛出异常，不影响 agent 执行链路。

7. **与 Story 5.1 状态机完全集成**：所有状态变更仍通过 `transitionTask()` 执行，心跳系统只读取状态，不写入状态。

8. **Prisma 迁移**：`Heartbeat` 模型需在实现前通过 `pnpm prisma migrate dev` 创建，迁移文件必须被提交。

9. **中文 UI 文本**：所有用户可见文本必须中文，`sanitizeError()` 处理错误消息。

### 核心实现目标

- Story 5.2 的核心是建立**长时间运行任务的连续性保证机制**，让任务在数小时执行期间不因时间长而失去可见性。
- 心跳系统是状态可信度的数据来源，但不替代状态机。
- 上下文快照为异常恢复提供历史锚点。
- UI 必须基于心跳状态展示可信度标记，不在"状态不可信"时误导用户。

### 默认实现决策

- **心跳调度在监督器进程内**：HeartbeatScheduler 运行在执行监督器进程内存中，通过定时器定期写入数据库。
- **快照策略 5 分钟一次**：只在运行中任务上保存上下文快照，每 5 分钟最多保存一条，避免频繁写入。
- **心跳分钟级去重采样**：轨迹展示时，同一分钟内的多条心跳只保留一条采样，避免列表爆炸。
- **状态真值在数据库**：即使 HeartbeatScheduler 重启，控制面仍能从数据库恢复最新状态。

### 前序 Story 情报（Story 5.1）

#### 状态机已建立的机制

- `transitionTask()` 是唯一的状态变更入口，写入 `TaskStateEvent` 记录。
- `Task.currentStage` / `Task.currentActivity` / `Task.nextStep` 在每次状态变更时由 `deriveStageContext()` 更新。
- `STATUS_LABELS` / `STATUS_SEMANTICS` 提供每个状态的中文标签和 UI 语义。

#### 生命周期已建立的机制

- `launch.ts` 负责从 `dispatched` → `starting` → `running` 的状态推进，创建 `ExecutionSession`。
- `lifecycle.ts` 负责从 `running` → `completed/failed/terminated` 的状态变更，清理 session。

#### 与 Story 5.2 的关系

- Story 5.2 **扩展**了 Story 5.1 的状态机，在状态机之上叠加了心跳和可信度层。
- Story 5.2 **不修改** Story 5.1 的状态转换规则和事件记录逻辑。
- Story 5.2 **不包含**异常检测和恢复（由 Story 6.1 负责），但为恢复提供心跳数据。

### 当前代码锚点

- `src/lib/execution/state-machine/types.ts` — 状态值、语义（Story 5.1 已建立）
- `src/lib/execution/state-machine/transitioner.ts` — 状态变更入口（Story 5.1 已建立）
- `src/lib/execution/state-machine/context.ts` — 阶段上下文（Story 5.1 已建立）
- `src/lib/execution/supervisor/launch.ts` — 会话启动，需接入 HeartbeatScheduler
- `src/lib/execution/supervisor/lifecycle.ts` — 会话结束，需停止 HeartbeatScheduler
- `src/lib/db/helpers.ts` — 数据库辅助查询，需添加分页方法
- `src/components/tasks/task-detail-view.tsx` — 任务详情 UI，需集成可信度标记
- `src/components/tasks/state-timeline.tsx` — 状态时间线，需支持长轨迹

### 推荐文件落点

```
src/lib/execution/
├── heartbeat/
│   ├── index.ts              # 导出所有 heartbeat API
│   ├── recorder.ts            # 心跳记录器（写 Heartbeat 表）
│   ├── detector.ts            # 心跳超时检测与可信度计算
│   ├── scheduler.ts           # 心跳调度器（HeartbeatScheduler 类）
│   ├── process-info.ts        # 进程信息获取
│   └── __tests__/
│       ├── recorder.test.ts
│       └── detector.test.ts
├── continuity/
│   ├── index.ts              # 导出所有连续性 API
│   ├── state-trust.ts        # 状态可信度计算
│   ├── continuity-manager.ts # 上下文快照管理
│   ├── reconnection.ts       # 断线重连状态恢复
│   └── __tests__/
│       └── continuity.test.ts
└── __tests__/
    └── trail.test.ts          # 长轨迹查询测试
```

### 测试要求

- 测试框架继续使用 Vitest。
- 必须覆盖心跳记录器的写入和可选字段处理。
- 必须覆盖心跳可信度的三种状态（trusted / stale / unknown）判定。
- 必须覆盖状态可信度在 UI 不同推荐模式下的行为。
- 必须覆盖轨迹查询的分页和去重采样。
- 必须覆盖断线重连状态恢复的正确性。

### 最新技术信息

- Next.js 16.1.6、React 19.2.3、Prisma 6.19.2、Zod 4.3.6 版本基线不变。
- 心跳写入使用 Prisma 标准 CRUD，不涉及事务（单条心跳写入失败不影响其他数据）。
- 上下文快照使用 Prisma transaction 确保 metadata 更新的一致性。
- UI 可信度标记遵循 UX 规范的"状态 + 原因 + 下一步"原则。

### 范围边界

**本 Story 包含：**

- ✅ 心跳系统（Heartbeat 模型、记录器、调度器、检测器）
- ✅ 状态可信度机制（trusted / stale / unknown 三级判定）
- ✅ 上下文快照（定期保存关键上下文到 Task.metadata）
- ✅ 断线重连状态恢复（绕过缓存获取最新状态）
- ✅ 长轨迹分页查询（状态事件和心跳采样的合并展示）
- ✅ UI 可信度标记集成（状态区警告横幅、心跳时间显示）

**本 Story 不包含：**

- ❌ 心跳超时的自动异常检测和恢复（由 Story 6.1 负责）
- ❌ SSE 实时推送（由 Story 5.3 负责）
- ❌ 执行日志的流式采集和展示（由 Story 5.3 / 5.8 负责）
- ❌ Agent 输出监听和交互请求识别（由 Story 5.3 负责）
- ❌ 心跳超时的自动通知和升级（由 Story 5.11 负责）
- ❌ 任务详情页的完整重构（只集成可信度相关的局部更新）

### Project Structure Notes

- 心跳模块放在 `src/lib/execution/heartbeat/`，连续性模块放在 `src/lib/execution/continuity/`。
- Heartbeat 模型写入高频数据但不进核心事务热表（与执行日志分离存储）。
- UI 组件只读取心跳和可信度数据，不直接写入。
- 上下文快照与状态变更分离：快照是状态变更的补充，不替代 `transitionTask()` 的核心职责。

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 5.2] — Story 5.2 原始用户故事与验收标准
- [Source: _bmad-output/planning-artifacts/epics.md#FR25] — 长时间运行任务状态连续（FR25）
- [Source: _bmad-output/planning-artifacts/epics.md#FR35] — 心跳、活动更新与状态可信度（FR35）
- [Source: _bmad-output/planning-artifacts/epics.md#Epic 5] — Epic 5 完整上下文
- [Source: _bmad-output/planning-artifacts/prd.md#NFR18] — 长时间运行任务（NFR18）
- [Source: _bmad-output/planning-artifacts/prd.md#NFR3] — 心跳状态延迟小于 15 秒（NFR3）
- [Source: _bmad-output/planning-artifacts/prd.md#NFR12] — 状态一致性检测（NFR12）
- [Source: _bmad-output/planning-artifacts/prd.md#NFR33] — 稳定状态交换（NFR33）
- [Source: _bmad-output/planning-artifacts/prd.md#NFR39] — 状态不可信显式暴露（NFR39）
- [Source: _bmad-output/planning-artifacts/architecture.md#Data Architecture] — 原始日志不进入高频事务热表
- [Source: _bmad-output/planning-artifacts/architecture.md#State Management Patterns] — 状态管理模式
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#Feedback Patterns] — 状态 + 原因 + 下一步
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#Execution Status Rail] — 状态展示组件规范
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#Journey 2] — 异常状态透明期望
- [Source: _bmad-output/implementation-artifacts/5-1-任务状态机与细粒度状态流转.md] — 前序 Story 5.1 完整上下文
- [Source: _bmad-output/implementation-artifacts/4-6-执行边界与上下文安全控制.md] — 执行边界上下文
- [Source: _bmad-output/project-context.md] — 项目约束与规范
- [Source: AGENTS.md] — 错误处理、Tailwind、Server Actions 规范
- [Source: src/lib/execution/state-machine/types.ts] — 状态值与语义（Story 5.1）
- [Source: src/lib/execution/state-machine/transitioner.ts] — 状态变更入口（Story 5.1）
- [Source: src/lib/execution/supervisor/launch.ts] — 会话启动，需接入心跳
- [Source: src/lib/execution/supervisor/lifecycle.ts] — 会话生命周期，需停止心跳
- [Source: prisma/schema.prisma] — 数据库模型，需添加 Heartbeat

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List
